<h1>{{ blog.title }}</h1>

{% for article in blog.articles %}
  <div>
    <a href="{{ article.url }}">
      <h2>{{ article.title | upcase }}</h2>
      <p>{{ article.author | upcase }}</p>
    </a>
  </div>
{% endfor %}