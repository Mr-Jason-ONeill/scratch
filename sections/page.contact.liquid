<h1>{{ page.title }}</h1>

<div>{{ page.content }}</div>

{% form 'contact', contact %}{% endform %}

{% schema %}
  {
    "name": "contact",
    "settings": [
      {
        "type": "text",
        "id": "contact",
        "label": "Contact"
      }
    ]
  }
{% endschema %}